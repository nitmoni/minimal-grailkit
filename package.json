{
  "name": "minimal-grailkit",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "concurrently \"yarn scss\" \"yarn serve\"",
    "scss": "sass --watch src/styles/style.scss dist/style.css",
    "serve": "eleventy --serve",
    "build": "eleventy"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2",
    "@11ty/eleventy-plugin-pug": "^1.0.0",
    "concurrently": "^9.2.0",
    "pug": "^3.0.3",
    "sass": "^1.89.2"
  }
}
